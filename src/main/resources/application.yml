spring:
  datasource:
    url: jdbc:postgresql://localhost:5434/spring_db
    username: yuza
    password: yuza@2024
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
    database: postgresql
    database-platform: org.hibernate.dialect.PostgreSQLDialect